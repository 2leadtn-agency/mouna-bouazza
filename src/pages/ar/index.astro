---
import "../../styles/global.css";
import SiteFonts from "../../components/SiteFonts.astro";
import SiteNav from "../../components/SiteNav.astro";
import SiteFooter from "../../components/SiteFooter.astro";
import ChatWidget from "../../components/ChatWidget.astro";
import SiteScripts from "../../components/SiteScripts.astro";

const icon = (name: string) => {
  const icons: Record<string, string> = {
    leaf: '<path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 4.18 2 8 0 5.5-4.78 10-10 10Z"/><path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12"/>',
    users: '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><path d="M16 3.128a4 4 0 0 1 0 7.744"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><circle cx="9" cy="7" r="4"/>',
    lock: '<rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>',
    "heart-handshake": '<path d="M19.414 14.414C21 12.828 22 11.5 22 9.5a5.5 5.5 0 0 0-9.591-3.676.6.6 0 0 1-.818.001A5.5 5.5 0 0 0 2 9.5c0 2.3 1.5 4 3 5.5l5.535 5.362a2 2 0 0 0 2.879.052 2.12 2.12 0 0 0-.004-3 2.124 2.124 0 1 0 3-3 2.124 2.124 0 0 0 3.004 0 2 2 0 0 0 0-2.828l-1.881-1.882a2.41 2.41 0 0 0-3.409 0l-1.71 1.71a2 2 0 0 1-2.828 0 2 2 0 0 1 0-2.828l2.823-2.762"/>',
    phone: '<path d="M13.832 16.568a1 1 0 0 0 1.213-.303l.355-.465A2 2 0 0 1 17 15h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2A18 18 0 0 1 2 4a2 2 0 0 1 2-2h3a2 2 0 0 1 2 2v3a2 2 0 0 1-.8 1.6l-.468.351a1 1 0 0 0-.292 1.233 14 14 0 0 0 6.392 6.384"/>',
    "map-pin": '<path d="M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0"/><circle cx="12" cy="10" r="3"/>',
    clock: '<circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/>',
    "message-circle": '<path d="M2.992 16.342a2 2 0 0 1 .094 1.167l-1.065 3.29a1 1 0 0 0 1.236 1.168l3.413-.998a2 2 0 0 1 1.099.092 10 10 0 1 0-4.777-4.719"/>',
    "arrow-down": '<path d="M12 5v14"/><path d="m19 12-7 7-7-7"/>',
    droplets: '<path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.1 3 12.25c0 2.22 1.8 4.05 4 4.05z"/><path d="M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 2.5 2 4.9 4 6.5s3 3.5 3 5.5a6.98 6.98 0 0 1-11.91 4.97"/>',
    waves: '<path d="M2 12q2.5 2 5 0t5 0 5 0 5 0"/><path d="M2 19q2.5 2 5 0t5 0 5 0 5 0"/><path d="M2 5q2.5 2 5 0t5 0 5 0 5 0"/>',
    flame: '<path d="M12 3q1 4 4 6.5t3 5.5a1 1 0 0 1-14 0 5 5 0 0 1 1-3 1 1 0 0 0 5 0c0-2-1.5-3-1.5-5q0-2 2.5-4"/>',
    quote: '<path d="M16 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z"/><path d="M5 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z"/>',
  };
  return icons[name] ?? "";
};
---
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="نور المنى بوعزة: عناية تكاملية للمرأة، تجمع بين المساج، الماديروثيرابي، التصريف اللمفاوي والحجامة، في فضاء خاص يحفظ خصوصيتكِ." />
  <title>نور المنى بوعزة — عناية تكاملية للمرأة</title>
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Ccircle cx='16' cy='16' r='16' fill='%23bd6c44'/%3E%3Ctext x='16' y='22' font-size='18' text-anchor='middle' fill='%23fbf8f2' font-family='Georgia,serif' font-style='italic'%3EM%3C/text%3E%3C/svg%3E" />
  <SiteFonts lang="ar" />
</head>
<body>

  <SiteNav lang="ar" />

  <main id="top">

    <!-- ============ HERO ============ -->
    <section class="hero">
      <div class="hero-text">
        <p class="eyebrow reveal">نور المنى بوعزة · عناية تكاملية للمرأة</p>
        <p class="hero-accent reveal reveal-delay-1">أهلًا بكِ</p>
        <h1 class="hero-title reveal reveal-delay-1">مساحة لكِ وحدكِ،<br />لتعودي إلى نفسكِ.</h1>
        <p class="hero-sub reveal reveal-delay-2">
          جلسات عناية تكاملية مخصّصة للنساء، تجمع بين المساج، الماديروثيرابي،
          التصريف اللمفاوي والحجامة، داخل فضاء هادئ يحفظ خصوصيتكِ ويمنحكِ
          الوقت والاهتمام اللذين تستحقينهما.
        </p>
        <div class="hero-actions reveal reveal-delay-3">
          <a href="#contact" class="btn btn-primary">احجزي لحظتكِ</a>
          <a href="#soins" class="btn-text">
            اكتشفي جلسات العناية
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("arrow-down")} />
          </a>
        </div>
      </div>
      <div class="hero-image reveal reveal-img">
        <img src="/images/hero-massage-pierres-chaudes-bougies.jpg" alt="جلسة تدليك بالحجارة الساخنة في أجواء شموع دافئة" />
      </div>
    </section>

    <!-- ============ SIGNATURE FULL-BLEED PARALLAX MOMENT ============ -->
    <section class="signature">
      <div class="signature-overlay"></div>
      <p class="signature-quote reveal">هنا، يهدأ كل شيء.</p>
      <p class="signature-text reveal reveal-delay-1">
        يخفّ ضجيج الخارج، يسترخي الجسد، وتجدين أخيرًا مساحة تتنفّسين فيها على مهل.
      </p>
    </section>

    <!-- ============ ABOUT / PHILOSOPHY ============ -->
    <section class="about container" id="a-propos">
      <div class="about-image reveal reveal-img">
        <img src="/images/mains-bienveillantes-soin-jambe.jpg" alt="أيادٍ حانية تقدّم عناية ملفوفة بمنشفة بيضاء" />
      </div>
      <div class="about-text">
        <p class="eyebrow reveal">عنايتكِ تبدأ بالإنصات إليكِ</p>
        <h2 class="reveal reveal-delay-1">لأن كل جسد يحمل قصة مختلفة</h2>
        <p class="reveal reveal-delay-2">
          لا تبدأ الجلسة بحركات محفوظة أو برنامج واحد يناسب الجميع، بل تبدأ
          بكِ أنتِ: بما تشعرين به، بما يحتاجه جسدكِ، وبالراحة التي تبحثين عنها.
        </p>
        <p class="reveal reveal-delay-2">
          تقدّم نور المنى بوعزة جلسات عناية تكاملية تجمع بين الممارسات
          التقليدية وتقنيات العناية الحديثة، ضمن مقاربة إنسانية تراعي
          خصوصية كل امرأة وإيقاعها.
        </p>
        <p class="reveal reveal-delay-2">
          هنا، لن تشعري بأنكِ رقم أو موعد عابر. ستجدين إنصاتًا حقيقيًا،
          عناية على مهل، وفضاءً صُمّم لتشعري فيه بالأمان والطمأنينة.
        </p>
        <p class="about-signature reveal reveal-delay-3">— نور المنى بوعزة</p>
      </div>
    </section>

    <!-- ============ SERVICES ============ -->
    <section class="soins" id="soins">
      <div class="container soins-head">
        <p class="eyebrow reveal">عناية تستمع إلى جسدكِ</p>
        <h2 class="reveal reveal-delay-1">اختاري اللحظة التي يحتاجها جسدكِ اليوم</h2>
        <p class="section-intro reveal reveal-delay-2">
          ليست كل الأيام متشابهة، وليست كل الأجساد بحاجة إلى العناية نفسها.
          لذلك تُختار كل جلسة وفق احتياجاتكِ، إحساسكِ والنتيجة التي تبحثين عنها.
        </p>
      </div>

      <div class="soin-row" data-side="left">
        <div class="container soin-grid">
          <div class="soin-media reveal reveal-img">
            <img src="/images/service-madero-therapie-outils-bois.jpg" alt="أدوات خشبية تُستخدم في جلسة الماديروثيرابي" />
          </div>
          <div class="soin-content reveal reveal-delay-1">
            <span class="soin-num">01</span>
            <svg class="icon soin-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("waves")} />
            <h3>الماديروثيرابي ونحت القوام</h3>
            <p>عناية تعتمد على أدوات خشبية وحركات مدروسة تستهدف مناطق محددة من الجسم، وتمنحكِ إحساسًا بالخفة والحيوية مع عناية خاصة بمظهر القوام والبشرة.</p>
            <p>جلسة للمرأة التي ترغب في الاعتناء بجسدها، استعادة نشاطها والشعور براحة أكبر في تفاصيله.</p>
          </div>
        </div>
      </div>

      <div class="soin-row" data-side="right">
        <div class="container soin-grid">
          <div class="soin-content reveal reveal-delay-1">
            <span class="soin-num">02</span>
            <svg class="icon soin-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("heart-handshake")} />
            <h3>المساج والاسترخاء</h3>
            <p>حين يحمل الجسد أكثر مما يستطيع، تصبح الراحة حاجة وليست رفاهية.</p>
            <p>جلسات مساج استرخائية أو موجهة، تُكيّف حركاتها ووتيرتها حسب احتياجاتكِ، لمساعدتكِ على تخفيف الإحساس بالتعب واستعادة لحظة من الهدوء العميق.</p>
          </div>
          <div class="soin-media reveal reveal-img">
            <img src="/images/service-massages-mains-dos.jpg" alt="تدليك للظهر بأيادٍ خبيرة ولطيفة" />
          </div>
        </div>
      </div>

      <div class="soin-row" data-side="left">
        <div class="container soin-grid">
          <div class="soin-media reveal reveal-img">
            <img src="/images/service-drainage-lymphatique-pied.jpg" alt="تصريف لمفاوي لطيف، أيادٍ موضوعة على القدم" />
          </div>
          <div class="soin-content reveal reveal-delay-1">
            <span class="soin-num">03</span>
            <svg class="icon soin-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("droplets")} />
            <h3>التصريف اللمفاوي</h3>
            <p>جلسة تتميز بحركات لطيفة، منتظمة ومدروسة، تمنح الجسم إحساسًا بالخفة والراحة، خاصة عند الشعور بالثقل أو الانتفاخ.</p>
            <p>عناية هادئة لا تستعجل جسدكِ، بل ترافقه بلطف وعلى إيقاعه الخاص.</p>
          </div>
        </div>
      </div>

      <div class="soin-row" data-side="right">
        <div class="container soin-grid">
          <div class="soin-content reveal reveal-delay-1">
            <span class="soin-num">04</span>
            <svg class="icon soin-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("flame")} />
            <h3>الحجامة</h3>
            <p>ممارسة تقليدية من ممارسات الطب التكميلي، تُقدَّم بعناية ودقة داخل أجواء هادئة تحترم راحتكِ وخصوصيتكِ.</p>
            <p>تبدأ كل جلسة بالاستماع إليكِ وفهم احتياجاتكِ، حتى تكون التجربة أكثر اطمئنانًا وملاءمة لكِ.</p>
          </div>
          <div class="soin-media reveal reveal-img">
            <img src="/images/service-hijama-bougie-plantes.jpg" alt="شمعة وقارورة عطارة وأزهار مجففة، أجواء مهدئة" />
          </div>
        </div>
      </div>
    </section>

    <!-- ============ REASSURANCE / EXPERIENCE ============ -->
    <section class="reassurance container" id="experience">
      <p class="eyebrow reveal">أكثر من مجرد جلسة عناية</p>
      <h2 class="reveal reveal-delay-1">كل ما حولكِ يقول: أنتِ في مكانكِ</h2>
      <p class="section-intro reveal reveal-delay-2">
        منذ لحظة دخولكِ، ستشعرين بأن هذا الفضاء لم يُصمّم فقط لتقديم جلسة، بل
        ليمنحكِ تجربة كاملة من الراحة، الخصوصية والاهتمام.
      </p>

      <div class="reassurance-grid">
        <div class="reassurance-card reveal reveal-delay-1">
          <span class="reassurance-icon">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("users")} />
          </span>
          <h4>فضاء مخصّص للنساء</h4>
          <p>مكان نسائي خالص، صُمّم لتكوني فيه مرتاحة، مطمئنة وعلى طبيعتكِ.</p>
        </div>
        <div class="reassurance-card reveal reveal-delay-2">
          <span class="reassurance-icon">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("lock")} />
          </span>
          <h4>خصوصيتكِ أولوية</h4>
          <p>كل جلسة تتم في أجواء خاصة وهادئة، بعيدًا عن الإزعاج ونظرات الآخرين.</p>
        </div>
        <div class="reassurance-card reveal reveal-delay-3">
          <span class="reassurance-icon">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("leaf")} />
          </span>
          <h4>جلسة على إيقاعكِ</h4>
          <p>لا استعجال، ولا تطبيق آلي للحركات. تُكيّف الجلسة مع حالتكِ واحتياجاتكِ في ذلك اليوم.</p>
        </div>
        <div class="reassurance-card reveal reveal-delay-4">
          <span class="reassurance-icon">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("heart-handshake")} />
          </span>
          <h4>الإنصات قبل العناية</h4>
          <p>نمنحكِ الوقت لتعبّري عما تشعرين به وما تنتظرينه من الجلسة، لأن العناية الحقيقية تبدأ عندما تشعرين أنكِ مسموعة.</p>
        </div>
      </div>
    </section>

    <!-- ============ PROMISE / BRAND MESSAGE ============ -->
    <section class="promise">
      <div class="container">
        <svg class="promise-quote-icon reveal" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("quote")} />
        <p class="eyebrow eyebrow-on-sage reveal">وعدنا لكِ</p>
        <p class="promise-quote reveal reveal-delay-1">لا نراكِ موعدًا في جدول.</p>
        <p class="promise-text reveal reveal-delay-2">
          نراكِ امرأة اختارت أن تتوقف قليلًا، أن تُصغي إلى جسدها، وأن تمنح
          نفسها العناية التي كثيرًا ما تؤجلها.
        </p>
        <p class="promise-text reveal reveal-delay-2">
          هنا، وقتكِ لكِ.<br />
          وراحتكِ تستحق كل اهتمامنا.
        </p>
        <p class="promise-sign reveal reveal-delay-3">— نور المنى بوعزة</p>
      </div>
    </section>

    <!-- ============ GALLERY / ESPACE ============ -->
    <section class="galerie container" id="espace">
      <p class="eyebrow reveal">مساحتكِ الخاصة</p>
      <h2 class="reveal reveal-delay-1">مكان يخفّ فيه الضجيج، وتبدأ الراحة من أول خطوة</h2>
      <p class="section-intro reveal reveal-delay-2">
        إضاءة دافئة، تفاصيل هادئة وأجواء نسائية حميمية صُمّمت لتشعري بالطمأنينة منذ دخولكِ.
      </p>
      <p class="section-intro reveal reveal-delay-2">
        بعيدًا عن برودة العيادات وضغط الحياة اليومية، تجدين مساحة تحتضن راحتكِ وتمنحكِ لحظة حقيقية مع نفسكِ.
      </p>

      <div class="galerie-grid">
        <div class="g-item g-tall reveal reveal-img">
          <img src="/images/ambiance-bassin-pieds-fleurs.jpg" alt="حوض للقدمين مزيّن بالزهور، طقس للعناية بالذات" />
        </div>
        <div class="g-item g-wide reveal reveal-img reveal-delay-1">
          <img src="/images/ambiance-cabine-soin-boheme.jpg" alt="غرفة علاج بأجواء دافئة ونباتية" />
        </div>
        <div class="g-item reveal reveal-img reveal-delay-1">
          <img src="/images/ambiance-pierres-chaudes-etagere.jpg" alt="حجارة ساخنة موضوعة على الظهر، أجواء لطيفة" />
        </div>
        <div class="g-item reveal reveal-img reveal-delay-2">
          <img src="/images/ambiance-etagere-huiles-bois.jpg" alt="رف خشبي وزيوت أساسية، ركن للعطارة" />
        </div>
      </div>
    </section>

    <!-- ============ CONTACT / BOOKING ============ -->
    <section class="contact" id="contact">
      <div class="container contact-grid">
        <div class="contact-text reveal">
          <p class="eyebrow eyebrow-on-sage">موعدكِ مع نفسكِ</p>
          <h2>هذه المرة، اختاري راحتكِ</h2>
          <p class="contact-lead-line">
            لا تنتظري حتى يتراكم التعب أو يصبح جسدكِ آخر ما تفكرين فيه.
          </p>
          <p class="contact-lead-line">
            امنحي نفسكِ لحظة تتوقفين فيها، تتنفّسين، وتستعيدين إحساسكِ بالهدوء والخفة.
          </p>
          <p class="contact-lead">
            نستقبلكِ بالموعد المسبق من الأحد إلى الخميس. يمكنكِ التواصل معنا
            عبر الهاتف أو واتساب لاختيار الجلسة والموعد الأنسب لكِ.
          </p>

          <ul class="contact-list">
            <li>
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("phone")} />
              <span class="contact-label">رقم الهاتف</span>
              <a href="tel:+21622959441" dir="ltr">+216 22 959 441</a>
            </li>
            <li>
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("map-pin")} />
              <span class="contact-label">الموقع</span>
              <span>العنوان الكامل يُرسل بعد تأكيد الموعد</span>
            </li>
            <li>
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("clock")} />
              <span>الاستقبال من الأحد إلى الخميس، بالموعد المسبق</span>
            </li>
          </ul>

          <a href="https://wa.me/21622959441" target="_blank" rel="noopener" class="btn btn-on-dark">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" set:html={icon("message-circle")} />
            احجزي موعدكِ عبر واتساب
          </a>
        </div>

        <div class="contact-accent reveal reveal-delay-1">
          <p>بكل عناية،<br />لأنكِ تستحقينها.</p>
        </div>
      </div>
    </section>

  </main>

  <SiteFooter lang="ar" />
  <ChatWidget lang="ar" />
  <SiteScripts />

<style>
  /* Arabic display + body typography for this locale */
  body { font-family: "Tajawal", "Segoe UI", sans-serif; }
  h1, h2, h3, h4 { font-family: "Amiri", serif; font-weight: 700; }

  .section-intro {
    color: var(--espresso-soft);
    font-size: 1.02rem;
    line-height: 1.85;
    max-width: 56ch;
  }

  /* ============ HERO ============ */
  .hero {
    display: grid;
    grid-template-columns: minmax(320px, 1fr) 1.35fr;
    align-items: center;
    min-height: 100vh;
    padding-top: 5rem;
    gap: clamp(1.5rem, 4vw, 3rem);
  }
  .hero-text {
    padding-right: clamp(1.25rem, 6vw, 5rem);
    padding-left: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.1rem;
  }
  .hero-accent {
    font-family: "Aref Ruqaa", serif;
    font-weight: 700;
    font-size: 1.7rem;
    color: var(--terracotta);
  }
  .hero-title {
    font-size: clamp(2.6rem, 5.2vw, 4.4rem);
    font-weight: 700;
    line-height: 1.25;
    color: var(--espresso);
    max-width: 15ch;
  }
  .hero-sub {
    font-size: 1.08rem;
    color: var(--espresso-soft);
    max-width: 42ch;
    line-height: 1.85;
  }
  .hero-actions {
    display: flex;
    align-items: center;
    gap: 1.8rem;
    margin-top: 0.6rem;
    flex-wrap: wrap;
  }
  .btn-text {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: var(--espresso);
    font-size: 0.95rem;
    font-weight: 500;
    border-bottom: 1px solid rgba(53, 41, 31, 0.3);
    padding-bottom: 2px;
    transition: border-color 0.3s ease, gap 0.3s ease;
  }
  .btn-text:hover { border-color: var(--espresso); gap: 0.7rem; }
  .hero-image {
    height: 100vh;
    align-self: stretch;
    overflow: hidden;
    border-radius: 0 0 140px 0;
  }
  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 980px) {
    .hero { grid-template-columns: 1fr; min-height: auto; padding-top: 7rem; }
    .hero-text { padding-inline: 1.5rem; order: 1; }
    .hero-image { height: 60vh; order: 2; border-radius: 0 0 70px 0; }
    .hero-title { max-width: none; }
  }

  /* ============ SIGNATURE — fixed-background parallax ============ */
  .signature {
    position: relative;
    min-height: 62vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    overflow: hidden;
    background-image: url('/images/salle-de-soin-ambiance-cocon.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
  }
  .signature-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(38, 47, 33, 0.35) 0%, rgba(38, 47, 33, 0.55) 100%);
  }
  .signature-quote {
    position: relative;
    z-index: 1;
    font-family: "Amiri", serif;
    font-weight: 700;
    color: var(--paper);
    text-align: center;
    font-size: clamp(1.7rem, 3.6vw, 2.8rem);
    line-height: 1.5;
    max-width: 20ch;
    padding-inline: 1.5rem;
  }
  .signature-text {
    position: relative;
    z-index: 1;
    font-family: "Tajawal", sans-serif;
    font-weight: 400;
    color: rgba(251, 248, 242, 0.88);
    text-align: center;
    font-size: clamp(1rem, 1.6vw, 1.2rem);
    line-height: 1.8;
    max-width: 34ch;
    padding-inline: 1.5rem;
  }

  @media (max-width: 780px) {
    .signature { background-attachment: scroll; }
  }

  /* ============ ABOUT ============ */
  .about {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: clamp(2rem, 6vw, 5rem);
    align-items: center;
    padding-block: clamp(5rem, 10vw, 8rem);
  }
  .about-image {
    border-radius: 220px 220px 24px 24px;
    overflow: hidden;
    aspect-ratio: 3 / 4;
  }
  .about-image img { width: 100%; height: 100%; object-fit: cover; }
  .about-text { display: flex; flex-direction: column; gap: 1.3rem; max-width: 44ch; }
  .about-text h2 { font-size: clamp(2rem, 3.4vw, 2.7rem); line-height: 1.3; }
  .about-text p { color: var(--espresso-soft); font-size: 1.02rem; line-height: 1.85; }
  .about-signature {
    font-family: "Aref Ruqaa", serif;
    font-weight: 700;
    font-size: 1.3rem;
    color: var(--terracotta-dark);
    margin-top: 0.4rem;
  }

  @media (max-width: 860px) {
    .about { grid-template-columns: 1fr; }
    .about-image { aspect-ratio: 4 / 3; border-radius: 140px 24px 24px 24px; }
  }

  /* ============ SOINS ============ */
  .soins { padding-block: clamp(3rem, 8vw, 6rem) 2rem; }
  .soins-head { max-width: 58ch; margin-bottom: clamp(2.5rem, 6vw, 4.5rem); display: flex; flex-direction: column; gap: 1rem; }
  .soins-head h2 { font-size: clamp(2rem, 3.4vw, 2.6rem); line-height: 1.3; }

  .soin-row { padding-block: clamp(2.5rem, 6vw, 4rem); }
  .soin-row:nth-child(odd) { background: var(--cream-deep); }

  .soin-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: center;
  }
  .soin-media { order: 1; }
  .soin-content { order: 2; }
  .soin-row[data-side="right"] .soin-media { order: 2; }
  .soin-row[data-side="right"] .soin-content { order: 1; }

  .soin-media { border-radius: 12px; overflow: hidden; aspect-ratio: 4 / 3; }
  .soin-row:nth-child(2) .soin-media,
  .soin-row:nth-child(4) .soin-media { aspect-ratio: 1 / 1; border-radius: 50% / 40%; }
  .soin-media img { width: 100%; height: 100%; object-fit: cover; }

  .soin-content { position: relative; padding-right: 0.2rem; }
  .soin-num {
    font-family: "Fraunces", serif;
    font-style: italic;
    font-size: 1.1rem;
    color: var(--terracotta);
    display: block;
    margin-bottom: 0.6rem;
  }
  .soin-icon { width: 1.9rem; height: 1.9rem; color: var(--sage); margin-bottom: 0.9rem; }
  .soin-content:hover .soin-icon { transform: translateY(-3px) scale(1.1); }
  .soin-content h3 { font-size: clamp(1.5rem, 2.4vw, 1.9rem); margin-bottom: 0.7rem; }
  .soin-content p { color: var(--espresso-soft); max-width: 44ch; line-height: 1.85; }
  .soin-content p + p { margin-top: 0.9rem; }

  @media (max-width: 780px) {
    .soin-grid { grid-template-columns: 1fr; }
    .soin-media, .soin-row[data-side="right"] .soin-media { order: 1; }
    .soin-content, .soin-row[data-side="right"] .soin-content { order: 2; }
    .soin-row:nth-child(2) .soin-media,
    .soin-row:nth-child(4) .soin-media { aspect-ratio: 4/3; border-radius: 12px; }
  }

  /* ============ REASSURANCE — card grid ============ */
  .reassurance { padding-block: clamp(4rem, 9vw, 7rem); }
  .reassurance h2 { font-size: clamp(1.9rem, 3.2vw, 2.4rem); margin: 0.9rem 0 1.2rem; line-height: 1.35; }
  .reassurance .section-intro { margin-bottom: 2.8rem; }

  .reassurance-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.4rem;
  }
  .reassurance-card {
    background: var(--paper);
    border-radius: 22px;
    padding: 2rem 1.6rem;
    box-shadow: 0 2px 14px rgba(53, 41, 31, 0.05);
    transition: transform 0.5s var(--ease-soft), box-shadow 0.5s var(--ease-soft);
  }
  .reassurance-card:hover {
    transform: translateY(-7px);
    box-shadow: 0 20px 36px rgba(53, 41, 31, 0.12);
  }
  .reassurance-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    background: var(--cream-deep);
    margin-bottom: 1.3rem;
    transition: background 0.4s var(--ease-soft), transform 0.4s var(--ease-soft);
  }
  .reassurance-card:hover .reassurance-icon {
    background: var(--blush);
    transform: scale(1.08) rotate(6deg);
  }
  .reassurance-icon .icon { width: 1.5rem; height: 1.5rem; color: var(--terracotta-dark); }
  .reassurance-card h4 { font-family: "Tajawal", sans-serif; font-size: 1.05rem; font-weight: 700; margin-bottom: 0.5rem; }
  .reassurance-card p { color: var(--espresso-soft); font-size: 0.94rem; line-height: 1.75; }

  @media (max-width: 980px) {
    .reassurance-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 560px) {
    .reassurance-grid { grid-template-columns: 1fr; }
  }

  /* ============ PROMISE ============ */
  .promise {
    background: var(--sage-deep);
    color: var(--paper);
    padding-block: clamp(4.5rem, 10vw, 7.5rem);
    text-align: center;
  }
  .eyebrow-on-sage { color: var(--blush); }
  .eyebrow-on-sage::before { background: var(--blush); }
  .promise-quote-icon {
    width: 2.1rem; height: 2.1rem;
    color: var(--blush);
    opacity: 0.55;
    margin: 0 auto 1.2rem;
    display: block;
  }
  .promise-quote {
    font-family: "Amiri", serif;
    font-weight: 700;
    font-size: clamp(1.7rem, 3.6vw, 2.7rem);
    line-height: 1.5;
    margin: 1.6rem auto 0;
    max-width: 22ch;
  }
  .promise-text {
    font-family: "Tajawal", sans-serif;
    color: rgba(251, 248, 242, 0.82);
    font-size: 1.05rem;
    line-height: 1.85;
    max-width: 40ch;
    margin: 1.2rem auto 0;
  }
  .promise-sign { margin-top: 1.7rem; color: var(--blush); font-family: "Aref Ruqaa", serif; font-weight: 700; font-size: 1.2rem; }

  /* ============ GALERIE ============ */
  .galerie { padding-block: clamp(4.5rem, 10vw, 7.5rem); }
  .galerie h2 { font-size: clamp(1.9rem, 3.2vw, 2.4rem); margin: 0.9rem 0 1.2rem; }
  .galerie .section-intro { margin-bottom: 0.7rem; }
  .galerie .section-intro:last-of-type { margin-bottom: 2.8rem; }

  .galerie-grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr 1fr;
    grid-template-rows: 220px 220px;
    gap: 1.1rem;
  }
  .g-item { overflow: hidden; border-radius: 10px; }
  .g-item img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.9s var(--ease-soft); }
  .g-item:hover img { transform: scale(1.06); }
  .g-tall { grid-row: 1 / 3; }
  .g-wide { grid-column: 2 / 3; grid-row: 1 / 3; border-radius: 10px 220px 10px 10px; }

  @media (max-width: 780px) {
    .galerie-grid { grid-template-columns: 1fr 1fr; grid-template-rows: 170px 170px 170px; }
    .g-tall { grid-row: auto; grid-column: auto; }
    .g-wide { grid-row: auto; grid-column: auto; border-radius: 10px; }
  }

  /* ============ CONTACT ============ */
  .contact { background: var(--sage-darker); color: var(--paper); padding-block: clamp(4.5rem, 10vw, 7.5rem); }
  .contact-grid { display: grid; grid-template-columns: 1.3fr 0.7fr; gap: 3rem; align-items: center; }
  .contact-text h2 { color: var(--paper); font-size: clamp(2rem, 3.6vw, 2.8rem); margin: 0.8rem 0 1.3rem; line-height: 1.35; }
  .contact-lead-line { color: rgba(251, 248, 242, 0.75); max-width: 46ch; line-height: 1.8; margin-bottom: 0.9rem; }
  .contact-lead { color: rgba(251, 248, 242, 0.75); max-width: 46ch; line-height: 1.8; margin-bottom: 2rem; }
  .contact-list { list-style: none; margin: 0 0 2.2rem; padding: 0; display: flex; flex-direction: column; gap: 1rem; }
  .contact-list li { display: flex; align-items: center; gap: 0.7rem; color: rgba(251, 248, 242, 0.88); flex-wrap: wrap; }
  .contact-list .icon { width: 1.3rem; height: 1.3rem; color: var(--blush); flex-shrink: 0; }
  .contact-list a { text-decoration: none; }
  .contact-list a:hover { color: var(--blush); }
  .contact-label { font-weight: 700; color: var(--blush); }

  .contact-accent {
    text-align: center;
    border-right: 1px solid rgba(251, 248, 242, 0.2);
    padding-right: 2rem;
  }
  .contact-accent p { font-family: "Aref Ruqaa", serif; font-weight: 700; font-size: 1.9rem; line-height: 1.5; color: var(--blush); }

  @media (max-width: 860px) {
    .contact-grid { grid-template-columns: 1fr; }
    .contact-accent { border-right: none; border-top: 1px solid rgba(251, 248, 242, 0.2); padding-right: 0; padding-top: 2rem; }
  }
</style>
</body>
</html>
